#include "monty.h"

/**
 * sta_nop - funtion is a joke that does nothing
 * @stack: points to nothing
 * @line_num : variable that will never initialise
 */

void sta_nop(stack_t **stack, unsigned int line_cnt)
{
        (void) stack;
        (void) line_cnt;
}
